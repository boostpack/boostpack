name: Release

on:
  push:
    branches:
      - main
      - next
      - '[0-9]+.x'

env:
  NX_DAEMON: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24.13.0
          cache: 'npm'

      - run: npm ci
      - run: echo "$(pwd)/node_modules/.bin" >> $GITHUB_PATH
      - run: nx run-many --verbose -t build

      - uses: actions/cache/save@v4
        with:
          path: .nx/cache
          key: nx-release-${{ github.sha }}

  lint:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.13.0
          cache: 'npm'

      - run: npm ci
      - run: echo "$(pwd)/node_modules/.bin" >> $GITHUB_PATH

      - uses: actions/cache/restore@v4
        with:
          path: .nx/cache
          key: nx-release-${{ github.sha }}

      - run: nx run-many --verbose -t lint

  test:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.13.0
          cache: 'npm'

      - run: npm ci
      - run: echo "$(pwd)/node_modules/.bin" >> $GITHUB_PATH

      - uses: actions/cache/restore@v4
        with:
          path: .nx/cache
          key: nx-release-${{ github.sha }}

      - run: nx run-many --verbose -t test

  publish:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24.13.0
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - run: npm ci
      - run: echo "$(pwd)/node_modules/.bin" >> $GITHUB_PATH

      - uses: actions/cache/restore@v4
        with:
          path: .nx/cache
          key: nx-release-${{ github.sha }}

      - run: git config user.name "github-actions[bot]"
      - run: git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Resolve release channel
        id: channel
        run: |
          BRANCH="${GITHUB_REF_NAME}"
          if [[ "$BRANCH" == "main" ]]; then
            echo "tag=latest" >> "$GITHUB_OUTPUT"
            echo "preid=" >> "$GITHUB_OUTPUT"
          elif [[ "$BRANCH" == "next" ]]; then
            echo "tag=next" >> "$GITHUB_OUTPUT"
            echo "preid=next" >> "$GITHUB_OUTPUT"
          else
            echo "tag=$BRANCH" >> "$GITHUB_OUTPUT"
            echo "preid=" >> "$GITHUB_OUTPUT"
          fi

      - name: Version
        run: |
          if [[ -n "${{ steps.channel.outputs.preid }}" ]]; then
            nx release version prerelease --preid=${{ steps.channel.outputs.preid }} --skip-publish --yes
          else
            nx release --skip-publish --yes
          fi

      - run: git push --follow-tags

      - name: Publish
        run: nx release publish --tag=${{ steps.channel.outputs.tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}